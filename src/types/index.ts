export interface NavItem {
    content: string;
    label: string;
    href: string;
};

export interface addressItem {
    icon: string;
    address: string;
};

export interface benifitItem {
    icon: string;
    title: string;
    description:string;
};

